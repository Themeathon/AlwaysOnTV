### Views
- [ ] Videos, Playlist and Games have a List view
- [ ] Make it possible to sort videos by title
- [ ] Add all local media should have a progress bar to show that stuff happens

### Videos
- [x] Local files need to generate thumbnails (ffmpeg)
- [ ] Re-generate thumbnails if they are deleted in the folders
- [x] Local files need to generate duration (ffmpeg)
- [x] Local files need better Video ID (not uuids but local-1++)
- [x] Video titles need to be shown completely
- [x] Thumbnails can be set manually for videos

### Games
- [ ] Make it possible to add multiple games without the dialog closing

### Playlist Queue
- [x] is currently broken, because it just skips all the videos in the queue

#### Misc
- [ ] Reduce scrolling in dialogs

### Database
- [ ] Fix all the errors with FOREIGN KEYS
- [x] Setup of the current Database is wrong