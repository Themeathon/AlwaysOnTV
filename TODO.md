### Views
- Videos, Playlist and Games have a List view

### Videos
- Local files need to generate thumbnails (ffmpeg)
- Local files need to generate duration (ffmpeg)
- Local files need better Video ID (not uuids but local-1++)
- Video titles need to be shown completely
- Thumbnails can be set manually for videos

### Playlist Queue
- is currently broken, because it just skips all the videos in the queue

#### Misc
- Reduce scrolling in dialogs

### Database
- Fix all the errors with FOREIGN KEYS
- Setup of the current Database is wrong